{% extends 'base.html.twig' %}

{% block title %}Actualite{% endblock %}

{% block body %}
    <h1 class="py-5 text-center mt-5">Actualite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ actualite.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ actualite.contenu }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ actualite.auteur }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ actualite.date ? actualite.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    {# seule les utilisateur de role user et admin peuvent écrire, modifier ou supprimer un message #}
    {% if is_granted('ROLE_USER') or is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_actualite_edit', {'id': actualite.id}) }}">edit</a> 
        {{ include('actualite/_delete_form.html.twig') }}
    {% endif %}

    {% for commentaire in actualite.commentaires %}
            <p>{{ commentaire.auteur }}</p>
            <p>{{ commentaire.date|date('d-m-Y') }}</p>
            <p>{{ commentaire.contenu }}</p>
        {% else %}
        <div>pas de commentaire sur cette article.</div>
     
    {% endfor %}

<<<<<<< HEAD
    {% if is_granted('ROLE_USER') %}
        <h2>poster un commantaire</h2>
        {{ form(comment_form) }}
    {% endif %}
=======
    {# {% if is_granted('ROLE_USER') %}
    <h2>poste un commantaire</h2>

    {{ form(comment_form) }}
    {% endif %} #}
>>>>>>> eb78c7024c776bfaa679ebe1532b2b0f65ffde27

    <a href="{{ path('app_actualite_index') }}">revenir à la liste</a>

    {# définir l'id de l'actualité en question pour pouvoir la passer en url: #}
    {% set actualiteId = actualite.id %} 
    
    <a href="{{ path('app_commentaire_new', {'id': actualiteId}) }}">Ajouter un commentaire</a>

{% endblock %}
